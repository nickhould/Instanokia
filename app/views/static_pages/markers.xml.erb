<?xml version="1.0"?>
 	<markers>
 		<% i=0 %>
		<% @array.each do |loc| %>
			<marker>
				<name><%= i +=1 %></name>
				<address>ADDRESS <%= i %></address>
				<lat><%= loc[:latitude] %></lat>
				<lng><%= loc[:longitude] %></lng>
			</marker>
		<% end %>

	</markers>